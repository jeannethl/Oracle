Control de Cambio Jueves 21/03/2024


///Show parameter SGA


subir SGA
		alter system set sga_max_size=6G scope=spfile;
		alter system set sga_target=6G scope=spfile;


----Interrumpir la conexion F5

		alter system switch logfile;

		alter system checkpoint; 


		Shutdown immediate

		startup nomount

		show parameter SGA

		alter database mount

		alter database open

		alter system switch logfile;




bajar y subir primero nomount "STARTUP nomount"
despues chequear el parametro "show parameter SGA"
despues mount "alter database mount"
y despues open " alter database Open"


alter system switch logfile current 3 veces despues de subir


INIT ESTA CREADO EN EL /TMP CON NOMBRE "initMDATOS_JK_200324.ora"

